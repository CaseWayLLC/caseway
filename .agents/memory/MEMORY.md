- [stripe-replit-sync gotchas](stripe-replit-sync-gotchas.md) — syncBackfill needs {object:"all"}; esbuild drops migration .sql (copy into dist); connector cred field is `secret` not `secret_key`.
- [solomatch typecheck under load](solomatch-typecheck-contention.md) — full tsc can starve to a hang while the 3 workflows run; clean Vite HMR transform of changed files is a reliable syntax-validity proxy.
- [Drizzle array param binding](drizzle-array-param-binding.md) — a JS array in sql`` expands to a (v1,v2) tuple, not an array param; build ARRAY[...]::text[] via sql.join. Fails at runtime, not typecheck.
- [object-storage server template gotchas](object-storage-strict-ts.md) — strict-TS json cast; upload endpoint ships unauthenticated (gate it); exempt /objects/ paths from http(s) URL validation.
- [Location search q filter](location-search-filter.md) — split autocomplete "City, State, Country", drop country tokens, match parts on word boundaries; whole-string substring never matches an address.
- [Caseway state→town search](caseway-state-town-search.md) — landing town picker is a bundled per-state dataset (no geocoding), full list virtualized not cmdk; why dataset + the gotchas.
- [Caseway keyless geocoding](caseway-keyless-geocoding.md) — no Maps key → fall back to OSM/Nominatim (once on submit); landing search ALWAYS requires resolved coords (reject county/state); guard stale-result race vs edited input.
- [Caseway demo mode](caseway-demo-mode.md) — demo attorneys gated by isDemo + app_settings flag (hidden not deleted); new public read paths must call getDemoMode(); test admin cookie by forging HMAC from SESSION_SECRET in shell.
- [Caseway sample vs demo attorneys](caseway-sample-attorneys.md) — ~62 non-demo legacy rows (NOT the demo-mode feature) were INTENTIONALLY bulk-archived pre-launch → directory intentionally empty, don't "fix"; seed names collide so key by id; ranking demo needs same-category attorneys within 25mi.
- [Caseway listing soft-delete](caseway-soft-delete.md) — admin delete sets archived_at (restore clears it); every public read + admin queue must exclude archived rows, stacking with the demo filter; permanent-delete only acts on archived rows.
- [Orval request-body type](orval-request-body-type.md) — POST body TS type is the OpenAPI schema name (e.g. `EventInput`), not `<Operation>Body` (that's the zod export).
- [Caseway Supabase auth](caseway-supabase-auth.md) — public browse account-free; listing writes need a Supabase Auth account; bearer verified via getAuthUserId (getUser+TTL, per-route, no middleware); admin via supabase.auth.admin.
- [Caseway firm-account approval](caseway-firm-accounts.md) — firm approval = service-role-only app_metadata.firmStatus (client can't forge); approved firm listings INSERT status="approved" (no review), which breaks "new rows are pending" test assumptions.
- [Google Places new-account 403](google-places-new-api.md) — classic Autocomplete 403s for accounts created after Mar 2025; use New Places API + enable "Places API (New)".
- [Caseway image asset sizes](caseway-image-assets.md) — Vite ships imported attached_assets/public images uncompressed; hero was 10MB & seed headshots 1.3MB each — resize/compress in place (keep seed .png names).
- [Single @types/react across workspace](types-react-dedup.md) — Expo (~19.1) + web (^19.2) = two @types/react copies → lucide "unrelated types" TS2322 in spinner.tsx; pin one via pnpm overrides.
- [Caseway brand identifiers](caseway-brand-identifiers.md) — brand label is capitalized `Caseway`; lowercase `caseway` internal identifiers (cookie, CSS class, analytics key, gmaps callback) must NOT be renamed; web slug stays `solomatch`. No "Counselly" brand exists.
- [Caseway SEO](caseway-seo.md) — static-SPA SEO: wouter routes + imperative <head> + JSON-LD + /api/sitemap.xml; client slugify MUST match server slugify; OG/JSON-LD images must be absolute.
- [Expo port-detection blocker](expo-port-detection.md) — expo workflow can report DIDNT_OPEN_A_PORT while Metro actually serves /status=200; confirm by running dev manually + curl; react-native-maps needs a .web.tsx fallback.
- [Caseway admin PIN login](caseway-admin-pin.md) — admin login is a server-verified PIN (ADMIN_PIN) w/ in-memory per-IP rate limit; ADMIN_USERNAME/PASSWORD env now dead config, don't rewire.
- [Caseway terms audit + verification](caseway-terms-verification.md) — terms stamped server-side on create+PATCH; termsAcceptedIp must never enter the Attorney response schema; barNumber optional in API/required client-side; verify is manual admin attestation.
- [Caseway county/state index](caseway-county-index.md) — case-insensitive `lower(col)=` filters need a functional index on `lower(state),lower(county)`; a plain column b-tree is ignored by the planner.
- [Tailwind shorthand padding override](tailwind-shorthand-padding-override.md) — `sm:p-6` overrides a base `pt-24` in the 640–767px range; use directional utils (px/pt) not the `p-*` shorthand when mixing axes.
- [Grid/flex child fixed-width overflow](grid-child-fixed-width-overflow.md) — a fixed-width child (e.g. a `w-[440px]` auth card) blows out a 1-col mobile grid; add `min-w-0 w-full` to children, prefer `w-full max-w-[X]`.
- [Leaflet fit circle under overlays](leaflet-fit-circle-under-overlays.md) — frame the search circle with fitBounds(toBounds(radius*2)) + asymmetric padding (clear header top, mobile sheet ~40vh bottom); fixed zoom clips it.
- [Caseway attorney marketing pitch](caseway-marketing-pitch.md) — listing-pitch.tsx pitch shows ONLY on the attorney funnel (sign-up/sign-in/signup intake); scoping is by import only — never add it to client discovery pages.
- [Caseway fixed-header overlay](caseway-fixed-header-overlay.md) — global Layout header is transparent until window scrolls; full-screen internally-scrolling views (results map) need `solidHeader` or it floats transparent over the map.
- [Caseway attorney ranking](caseway-attorney-ranking.md) — sort order lives in 3 spots (server /attorneys + /similar orderBy, home.tsx client distance sort); a ranking rule (Pro-first) must be in all.
- [Caseway visibility gate spread](caseway-visibility-gate-spread.md) — apply the paid-posts+demo gate in EVERY public read path; the seo.ts sitemap is the easy-to-miss one (separate file from attorneys.ts).
- [react-leaflet GeoJSON types](react-leaflet-geojson-types.md) — @types/geojson only transitive; use ambient GeoJSON.* types + alias the react-leaflet GeoJSON value import to avoid shadowing; key={sig} to restyle.
- [Drizzle SELECT-projection self-alias](drizzle-select-projection-self-alias.md) — correlated count over a self-aliased table: ${table.id} renders unqualified in SELECT → binds to inner alias → silently 0; pin outer to literal "attorneys"."id".
- [Caseway referrals](caseway-referrals.md) — referredById self-FK captured at signup (validated, no self-referral), immutable after create; referralCount boosts rank as tier-2 below Pro; admin leaderboard GET /admin/referrals.
- [Caseway account types](caseway-account-types.md) — solo=1 listing/firm=many via user_metadata (missing=solo); cap enforced at POST /attorneys in an advisory-lock tx; route-test mocks must export getAuthUser+getAccountType.
- [executeSql targets Replit DB not Supabase](executesql-targets-replit-db.md) — code_execution executeSql/checkDatabase hit DATABASE_URL (Replit's mostly-empty DB), NOT app data in SUPABASE_DB_URL; use psql "$SUPABASE_DB_URL" for real rows.
- [Caseway GA4 Data API](caseway-ga4-data-api.md) — server reads GA4 (prop 542202072) via a service-account; live cred is `GA4_CREDENTIALS_JSON` (NOT the stale `GA4_SERVICE_ACCOUNT_JSON` secret, which the agent can't delete); needs Data API enabled + service-acct as Viewer; verify via raw RS256 JWT in bash.
- [Caseway saved & compare](caseway-saved-compare.md) — visitor shortlist is localStorage-only (no DB); /saved re-fetches public getAttorney w/ retry:false; nested button in clickable Card must stopPropagation on BOTH click AND keydown.
- [video-js build gotchas](video-js-build-gotchas.md) — audio callbacks can 401 (ship silent, don't retry); fresh video-js tsconfig omits the DOM lib so tsc fails on scaffold until you add lib:[dom,dom.iterable].
- [Artifact-managed workflow removal](artifact-managed-workflow-removal.md) — removeWorkflow is PROHIBITED for `artifacts/<slug>:` workflows; remove an artifact by `rm -rf artifacts/<slug>` (platform deregisters it) + pnpm install + typecheck.
- [Playwright live-site walkthrough recording](playwright-walkthrough-recording.md) — record a real demo video of a running artifact via playwright-core + system Chromium + ffmpeg webm→mp4; persisted as scripts `record:walkthrough` (.mjs to skip tsc).
- [Marketing HTML → PDF rendering](marketing-html-to-pdf.md) — marketing/*.html brand docs render to PDF via playwright-core + system Chromium page.pdf (throwaway scripts/src/*.mjs); wait for document.fonts.ready; footer needs explicit font-size.
- [Caseway homepage motion model](caseway-homepage-motion.md) — home step transitions: keep ONE translate source (the step slide); inner content fades only; never translate the map or backdrop-blur/shadow cards.
- [Caseway lazy map bundling](caseway-lazy-map-bundling.md) — import map only via attorney-map-lazy + Suspense; one static importer re-bundles ~50KB leaflet into that route; verify in index.html modulepreload.
- [pnpm add pulls duplicate vite](pnpm-add-vite-dup.md) — adding any dep refreshes the lockfile + can pull a 2nd vite patch that breaks mockup-sandbox/solomatch tsc; fix with `pnpm dedupe` + `pnpm install`.
- [Vite manualChunks vs modulepreload](vite-manualchunks-modulepreload.md) — naming an async-only lib in manualChunks makes Vite modulepreload it on the entry; only name entry-path vendors, verify dist index.html preloads after every chunking change.
